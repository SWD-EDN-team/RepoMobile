import { View, Text } from "react-native";
import { Link } from "expo-router";
const AuthTab = () => {
  return (
    <View>
      <Text>AuthTab Tabss pagse</Text>
      {/* <Link href={"/like"}>go to quandba</Link> */}
    </View>
  );
};

export default AuthTab;
